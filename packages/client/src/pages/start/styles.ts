import styled from 'styled-components'
import { Space, Button, Typography } from 'antd'

export const StartSpace = styled(Space)`
  margin-top: 2rem
`
export const StartButton = styled(Button)`
  margin-top: 2rem
`
export const StartTypography = styled(Typography)`
  cursor: pointer;
`