import styled from 'styled-components';
import { Space, Typography } from 'antd';

export const ColumnPlace = styled(Typography.Text)`
  font-size: 2rem;
  font-weight: 500;
  margin: 0 !important;
`;

export const ColumnUser = styled(Space)`
  gap: 1rem !important;
`;
