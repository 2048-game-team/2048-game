import styled from 'styled-components';
import { Layout } from 'antd';

export const FullLayout = styled(Layout)`
  height: 100%;
  width: 100%;
  max-width: 50rem;
  margin: 1rem auto;
  padding: 0 1rem;
`;
